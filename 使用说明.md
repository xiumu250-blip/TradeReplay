# K线复盘 · 交易模拟器 —— 使用说明

> 本工具是一个纯前端行情复盘训练器：导入历史K线后逐根重演行情，在「看不到未来」的环境里练习下单、止损止盈与画线分析，平仓记录自动保存为历史档案，随时回看每笔交易的K线波段并写下复盘备注。
> 界面默认**深色交易终端风格**（对标 TradingView 的克制配色与紧凑控件），并提供**白天 / 黑夜主题切换**（顶栏 🌙/☀ 按钮，自动记忆），不联网、不用服务器，双击 `index.html` 即用。

---

## 一、使用前的准备与环境要求

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome / Edge / Firefox 等现代浏览器（近两年版本均可），无需安装任何插件 |
| 网络 | **不需要联网**，所有功能本地运行 |
| 服务器 | **不需要**，直接双击 `index.html` 即可打开 |
| 文件 | 保持 `index.html`、`css/`、`js/` 的相对位置不变（整个文件夹一起拷贝/移动） |
| 数据（可选） | CSV 格式的历史K线。没有数据也能先用内置「示例数据」练习 |

**CSV 数据格式要求：**

- 共 6 列，顺序为：`时间, 开盘价, 最高价, 最低价, 收盘价, 成交量`
- 第一行表头**可有可无**（程序自动识别；有表头时支持中英文字段名，如 `date/open/close/成交量` 等）
- 分隔符支持逗号 `,`、Tab、分号 `;`、竖线 `|`，自动识别
- 时间支持：`2024-01-02 09:30:00`、`2024/01/02`、Unix 时间戳（秒或毫秒）
- 至少 2 行数据；时间乱序会自动排序

不确定格式时，先点顶栏「**CSV模板**」下载一份标准样例对照。

---

## 二、操作步骤

### 第 1 步：打开页面

**操作**：双击项目文件夹中的 `index.html`（若双击后被编辑器抢占，右键文件 → 打开方式 → 选择浏览器）。

**预期结果**：页面加载完成后**自动载入内置的 BTCUSDT 5 分钟历史数据**（2020-2026，约 68.9 万根K线），图表显示前 140 根，顶栏数据信息为「BTCUSDT · 5分 · 689189根 · 5分」，**图表上方的回放控制条**显示当前位置，右侧默认停在「持仓」页。

**注意**：
- 图表当前只显示到第 61 根，右侧大片空白是**故意留的**——那就是"未来"，回放会一根根把它走出来。
- 成交量副图默认**不显示**，需要显示请在工具栏勾选「成交量」。
- 顶栏「帮助」按钮可随时查看简版说明（覆盖全部功能）。

---

### 第 2 步：载入/切换数据

页面打开时已自动载入 **BTCUSDT** 2020-2026 数据，可直接开始复盘。如需切换：

- **切换内置标的**：点顶栏「标的」下拉框，可在 **BTCUSDT / XAU（黄金现货）/ ES500（标普 500 期货）** 三个内置数据集间切换。切换后交易、回放、历史记录均重置为该数据集的独立档案；
- **用示例数据**：点顶栏「示例数据」，载入一段合成的 1 小时示例K线（适合快速练手）；
- **用自己的 CSV**：点「导入 CSV」选择文件，或**直接把 CSV 文件拖到图表区域**（出现虚线框后松手）。

**预期结果**：顶栏数据信息变为「文件名 · N 根K线 · 周期」，图表与回放位置重置到起点，周期下拉框根据数据自动列出可选周期，历史成交记录切换为该数据集自己的档案。

**常见问题**：
- 提示「未能解析出K线数据」→ 检查是否少于 2 行、列数是否够 6 列、时间列能否被识别（Excel 另存 CSV 时时间常变成 `2024/1/2 0:00`，这是支持的；若是纯数字序号列则不行）。
- 导入后 K 线"挤成一条线" → 正常现象，滚轮缩放或拖动平移即可。

---

### 第 3 步：切换K线周期

**操作**：顶栏「周期」下拉框选择目标周期。

**可选周期**：**图表初始周期**（默认三个内置标的均为 5 分钟），以及 **5分、1时、1天、1周** 四档。

**规则**：
- 周期换算按基础周期自动计算（例如基础是 1 分钟 → 5分=5合1、1时=60合1）；聚合后不足 30 根K线的选项自动隐藏，避免"切过去只有几根线"。
- 切换是前端聚合，**原始数据不变**，随时切回初始周期。
- 有持仓/挂单/成交时切换会弹确认（切换会重置回放与交易状态），已保存的历史成交记录不受影响。

---

### 第 4 步：回放行情（控制条在图表上方）

**基本操作**：
- **播放 / 暂停**：点 ▶ 按钮，或按 `=` 键；
- **下一根**：`空格` 键（**只前进不回退，已取消「上一根」，不允许回退到上一根**）；
- **速度**：1x / 2x / 5x / 10x；
- **进度条**：拖到任意位置。向前拖逐根撮合挂单与止盈止损；**向后拖自动回滚交易状态**（快照机制，仍可回看任意位置，但单步回退上一根已取消）；
- **回到起点**：左侧 ⏮ 按钮。

**起点设置**：
- **随机起点**：点「🎲 随机起点」，随机跳到行情中间某处重新开始，适合反复训练同一段行情；
- **起始日期**：在日期框选择某天，回放从该日期（含）之后的第一根K线开始。日期框的日历图标跟随深色主题显示为白色，选择后仍保持可见。

**跟随行为**：播放/下一根时自动跟随最新K线——**仅在最新K线位于固定位置（屏幕右侧锚点）右侧时才回卷到该固定位置，位于左侧则保持视口不动**；如果手动平移了图表则暂停跟随（方便回看左边），**再次按播放自动恢复跟随**。

---

### 第 5 步：快捷下单

本工具主打快捷下单，所有下单数量固定为 **1 手**，可通过以下三种方式：

**方式一：工具栏（最直观）**
图表上方工具栏右侧有：
- **市价买 / 市价卖**：立即按当前K线收盘价成交；
- **全平**：市价平掉全部持仓；
- **撤单**：撤销全部未成交挂单；
- **清空**：清空当前回放的交易状态（不影响已保存的历史记录）。

**方式二：图表右键（推荐）**
在图表**空白位置**右键，菜单只显示**两个精简下单项（不含价格信息）**，方向由右键价格与现价的关系自动决定：
- 右键价格 **高于** 现价 → 「限价做空」（反弹到该价成交）＋「突破做多」（涨破该价成交）；
- 右键价格 **低于** 现价 → 「限价做多」（回落到该价成交）＋「突破做空」（跌破该价成交）。

点击即在鼠标位置的价格挂单，数量固定为 1 手。右键点在**已有挂单线**上时，菜单只显示「撤销此挂单」一项，不会弹出下单入口。

**方式三：快捷键**
- `B` 市价做多 1 手；
- `S` 市价做空 1 手；
- `C` 全部平仓。

---

### 第 6 步：设置止盈止损（拖线即设）

- 开仓后图表出现**蓝色开仓线**：直接**上下拖动**它——拖到开仓价的盈利一侧松手即设**止盈**，亏损一侧即设**止损**（做空方向相反），松手前有实时预览；
- 橙色虚线是挂单，同样可以直接拖动改价；
- 红色虚线=止损、紫色虚线=止盈，都可以拖动调整；
- 也可在「持仓」页签的卡片里精确填写价格后保存；持仓卡片会**同时显示浮动盈亏的金额与百分比**（如 `+120.50 (+2.15%)`，盈红亏绿），随最新价实时刷新。

**撮合规则**：按整根K线判定，跳空按开盘价成交；同一根K线同时触及止损与止盈时保守判为**先止损**；开仓当根不判止盈止损。

---

### 第 7 步：历史成交记录（自动持久化）

**右侧「历史」页签**：每笔平仓自动记录并**保存在浏览器本地**——关闭页面后重新打开仍然可以查看。每条记录包含：

- 方向（多/空）与数量；
- 开仓价 → 平仓价、平仓时间；
- 盈亏金额（红=盈利、绿=亏损）；
- 备注摘要（写了备注的记录会显示前 40 字）。

**点击任意一条记录**打开详情弹窗：
- **K线波段图**：该笔交易对应的K线区间（开仓前 10 根到平仓后 10 根），带开仓箭头、平仓叉号与开平仓虚线连线，右侧标注开/平价格；
- **交易信息**：开平仓价格与时间、平仓原因、持有根数、手续费、当时所用周期；
- **备注框**：写下复盘心得（入场理由、错误教训等），点「保存备注」持久保存；
- **跳到平仓处**：把回放位置直接跳到这笔交易平仓的那根K线，方便对照主图复盘。跳转后**主图表会自动标注进出场**：在**进场点**画箭头（多=向上红三角、空=向下绿三角）、**平仓点**画叉号，并以虚线连线串起开平仓路径（红=盈利、绿=亏损）；若进场点不在当前画面，视口会自动回卷把进场点纳入视野，便于直观回溯当时的操作路径。

**管理**：「历史」页签右上角「清空」可删除当前数据集的全部历史记录（需确认，不可恢复）。「清空交易」按钮只重置当前回放的交易状态，**不影响已保存的历史记录**。

---

### 第 8 步：画线分析（两击交互）

**绘制**：所有画线工具统一为**两段式点击**交互——工具栏选择工具后——
- **线段 / 射线 / 斐波回撤**：单击确定起点，再次单击确认位置并结束；
- **斐波扩展（AB=CD）**：依次单击 A、B、C 三个点，从 C 按定向价差 B−A 投影出 0.618 / 1(AB=CD) / 1.618 / 2 / 2.618 比例目标位；
- **做多盈亏 / 做空盈亏**：**首次点击**位置即固定为**开仓价**（紫色虚线）并确定矩形左边界，同时立即显示**默认止盈/止损**初始距离（做多：止盈在上/止损在下；做空：止盈在下/止损在上）；随后鼠标**向右移动**实时拉伸矩形预览，**再次点击**确认矩形右边界成图。确认后可**自由拖动止盈线（红）/ 止损线（绿）**：垂直拖动调整该线价位、水平拖动移动矩形右边界（右边界不会越过开仓列），**开仓线与左边界保持固定**；成图后自动检测**止盈/止损价位在区间内首次被K线触发（打到）的位置**作为**离场位置**，以**虚线绘制「开仓 → 止盈离场」「开仓 → 止损离场」两条连线**（区间内未触发则该条连线不绘制）；止盈区为淡透明红、止损区为淡透明绿（盈红亏绿）；
- **水平线 / 文字 / 箭头**：单击一次即放置（文字在**图表上直接弹出输入框**输入，不会触发浏览器对话框、全屏状态下也不退出全屏，输入一次后自动切回光标模式；↑箭头=看涨、↓箭头=看跌，均为**蘑菇状标记：三角箭头头部＋矩形箭身**）；
- **Shift 锁定水平**：绘制**线段 / 趋势线 / 射线**时按住 `Shift`，第二点价格自动对齐首个点，仅沿水平轴绘制（忽略垂直偏移），绘制前有实时水平预览；
- 每次「点击」= 按下后原位松开（指针基本不动）。**按住拖动不成图**（仅实时预览），须在目标处干净地点击一次确认该点——旧的「按下起点、松开确认（拖拽成图）」交互已取消；
- 绘制过程中鼠标移动有实时预览，`Esc` 取消。

**颜色与线型**：色板选预设颜色，或用色板末尾的**取色器**自定义任意颜色；「实线/虚线」按钮切线型——设置**按工具独立记忆并自动保存**，下次打开自动恢复。选中已有画线时改动颜色/线型会同步应用到该画线。

**编辑**：
- 单击选中，拖动画线本体移动整体，拖动端点（小方块）调整锚点；
- `Delete`（或「删除」按钮）删除选中画线、「清空」删除全部画线——两者均**不弹确认框、无提示**；
- **双击**画线：文字→改内容；水平线→改标签；**斐波回撤/扩展→打开比例设置弹窗**，可逐条增删改比例、选用预设（默认回撤 / 默认扩展 / 纯 AB=CD），勾选「保存为默认」后新画线会自动使用这套比例。

---

### 第 9 步：自定义快捷键

工具栏「快捷键」按钮打开设置弹窗：
- 点击某个操作的按键框 → 按下想绑定的键 → 完成；
- 可绑定：**播放/暂停、下一根、全屏**（`B`/`S`/`C`/`空格`/`=` 为固定快捷键，始终生效，不在弹窗内；已取消「上一根」）；
- 按键冲突会提示；`Delete`/`Esc` 为固定功能不可绑定；
- 「保存」后立即生效并持久保存；「恢复默认」回到 =/空格/F。
- 「统计」页签底部会实时显示当前绑定。

**固定快捷键**：`B` 市价买入 1 手、`S` 市价卖出 1 手、`C` 全部平仓、`=` 播放/暂停、`空格` 下一根K线（只前进不回退）。

---

### 第 10 步：查看统计与权益曲线

「统计」页签：平仓笔数、胜率、总盈亏、盈亏比、盈利因子、平均盈亏、最大单笔盈亏、最大回撤、盈亏笔数、浮动盈亏、总权益，以及**已实现权益曲线**（面积图，红点为当前权益）。右侧栏仅保留**持仓 / 历史 / 统计**三个页签；挂单仍可在图表上通过拖动橙色委托线管理，不再单独列页。

---

### 第 11 步：图表视图操作

| 操作 | 方式 |
|------|------|
| 横轴缩放 | 滚轮 |
| 平移 | 按住图表空白处拖动 |
| 纵轴缩放 | **右侧价格轴上下拖动**（向上=放大） |
| 纵轴复位 | 双击价格轴 |
| 成交量副图 | 工具栏「成交量」开关，默认关闭；副图右上角 ✕ 关闭后主图填满 |
| EMA20 | 工具栏「EMA20」开关，默认开启，以**金黄色（gold）**线显示；开关旁的**取色器**可自定义 EMA20 颜色（自动记忆） |
| 日期分割线 | 日线交界处自动绘制竖直虚线，区分每日数据区间 |
| 当前K线位置 | 仅显示右侧「最新价」标签，**不再绘制竖直虚线** |
| 顶部行情条 | 图表上方仅显示当前K线时间（悬停某K线时显示其时间），不显示开/高/低/收/量 |
| 成交标记 | 工具栏「成交标记」开关（▲/▼ 开仓、✕ 平仓、虚线连线） |
| 全屏 | 工具栏「全屏」或 `F` 键 |

---

## 三、键盘与鼠标速查

### 键盘（可在「快捷键设置」中自定义）

| 按键 | 默认功能 |
|------|----------|
| `=` | 播放 / 暂停（固定） |
| `空格` | 播放下一根K线，只前进不回退，无「上一根」（固定） |
| `B` / `S` | 市价买入 / 卖出 1 手（固定） |
| `C` | 全部平仓（固定） |
| `F` | 图表全屏 / 退出 |
| `Delete` | 删除选中的画线（固定） |
| `Esc` | 取消画线 / 关闭菜单与弹窗（固定） |

### 鼠标

| 操作 | 功能 |
|------|------|
| 右键图表空白处 | 仅显示「限价做多/做空 · 突破做多/做空」两项下单（不显示价格） |
| 右键挂单线 | 仅显示「撤销此挂单」一项 |
| 拖动蓝色开仓线 | 松手设止盈/止损 |
| 拖动挂单/止损/止盈线 | 直接改价 |
| 拖动画线/端点 | 移动/调整画线 |
| 双击画线 | 编辑文字/标签/斐波比例 |
| 双击价格轴 | 纵轴缩放复位 |
| 拖入 CSV 文件 | 导入数据 |

---

## 四、常见问题（FAQ）

**Q1：右键菜单为什么这么精简？**
图表空白处右键只保留跟随价格位置有意义的方向：高于现价是「限价做空 + 突破做多」，低于现价是「限价做多 + 突破做空」，且菜单不显示价格（价格就是鼠标所指位置），避免下错方向；右键点在已有挂单线上则只显示「撤销此挂单」一项。

**Q2：周期下拉里没有 5分/1天/1周？**
周期按数据的基础周期换算，聚合后不足 30 根的选项自动隐藏。例如 720 根 1 小时数据只显示「初始·1时」和「1日」；想要周线就导入更长的数据。

**Q3：平仓后记录在哪里？**
右侧「历史」页签，自动保存在浏览器本地。注意：用浏览器的「无痕模式」或清理浏览器数据会导致记录丢失。

**Q4：切换数据后历史记录还在吗？**
在。历史记录按数据集名称（文件名）分开保存，重新导入同名文件即可看到之前的记录。

**Q5：为什么我改了快捷键不生效？**
检查是否处于输入框/下拉框聚焦状态（此时快捷键禁用）；确认在设置弹窗点了「保存」。

**Q6：画线怎么保存？**
不需要手动保存——颜色和线型**按工具自动保存**；画线本身随图表存在（刷新页面清空），这是设计如此，保持图表干净。

**Q7：回放到头了怎么办？**
按播放会自动回到起点重新开始；也可以用「随机起点」换一段行情。

**Q8：向回拖进度条，交易会怎样？**
自动回滚到那个时间点时的持仓/挂单/资金状态（快照机制），可以反复重演同一段行情练手。已平仓的交易仍保留在历史记录中。

**Q9：限价/Stop 单什么时候成交？**
后续K线触及价格时按整根K线判定成交，跳空按开盘价成交。挂单在开仓当根不会成交。

**Q10：拖开仓线设止盈止损的方向？**
拖到**盈利一侧**=止盈，**亏损一侧**=止损（做空相反）。松手前有预览提示，不会误设。

**Q11：全屏后按 Esc 退出了全屏但页面没反应？**
浏览器全屏下 Esc 先退出全屏，再按一次 Esc 才会触发取消画线等页面功能，属正常现象。

**Q12：支持哪些数据？**
页面内置 **BTCUSDT / XAU / ES500** 三个标的，默认显示 BTCUSDT。也支持任何符合格式要求的 CSV（股票、期货、外汇、加密货币均可），时间为升序或乱序都可（自动排序）。

---

## 五、文件结构

```
├── index.html          主页面（双击打开）
├── css/style.css       样式（深色交易终端风格）
├── js/data.js          CSV 解析 / 周期聚合 / 示例数据
├── js/chart.js         K线图引擎（Canvas 绘制）
├── js/drawings.js      画线工具（线段/射线/水平线/斐波/文字）
├── js/trading.js       撞合撮合引擎（订单/持仓/止盈止损/统计）
├── js/app.js           主程序（回放/交互/面板/历史记录/快捷键/数据集切换）
├── data/               内置数据集（btcusdt.js / xau.js / es500.js）
├── test_runtime.js     Node 运行时回归测试（98 项）
└── 使用说明.md          本文档
```
